// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package ugs.actions;

import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;
import com.mendix.systemwideinterfaces.core.IMendixObject;

public class LondAns_WordCount extends CustomJavaAction<java.lang.Long>
{
	private IMendixObject __LongAnswer;
	private dynamicform.proxies.LongAnswer LongAnswer;

	public LondAns_WordCount(IContext context, IMendixObject LongAnswer)
	{
		super(context);
		this.__LongAnswer = LongAnswer;
	}

	@java.lang.Override
	public java.lang.Long executeAction() throws Exception
	{
		this.LongAnswer = __LongAnswer == null ? null : dynamicform.proxies.LongAnswer.initialize(getContext(), __LongAnswer);

		// BEGIN USER CODE
		String string=this.LongAnswer.getLongAnswer_Value(this.getContext());
		long count=0;
		char ch[]= new char[string.length()];     
        for(int i=0;i<string.length();i++)  
        {  
            ch[i]= string.charAt(i);  
            if( ((i>0)&&(ch[i]!=' ')&&(ch[i-1]==' ')) || ((ch[0]!=' ')&&(i==0)) )  
                count++;  
        }  
        return count ;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@java.lang.Override
	public java.lang.String toString()
	{
		return "LondAns_WordCount";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
